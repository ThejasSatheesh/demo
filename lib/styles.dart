import 'package:flutter/material.dart';

class textStyles {
  static const nameStyle = const TextStyle(
    fontSize: 40,
    color: Colors.white,
    fontWeight: FontWeight.bold,
    fontFamily: "farro",
  );

  static const locationStyle = const TextStyle(
    fontSize: 25,
    fontFamily: "farro-light",
    color: Colors.white,
  );
}
